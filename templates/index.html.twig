<!DOCTYPE html>
<html lang="pl">
<head>
	<meta charset="utf-8">
	<title>Fuero</title>
	<link href="components/bootstrap-default/css/bootstrap.min.css" type="text/css" rel="stylesheet" media="screen" />

	<meta name="viewport" content="width=device-width">
</head>
<body>
	<div class="container-fluid">
		<div class="page-header">
			<p class="lead">Rekrutacyjne zadanie testowe dla Fuero</p>
		</div>
		<div class="row">
			<div class="col-md-5 col-md-offset-4">
				<div class="jumbotron">
					<form role="form" method="POST">
						<div class="form-group{% if error != '' %} has-error{% endif %}">
							<label for="url">Podaj adres url{% if error != '' %} - {{ error }}{% endif %}</label>
							<input type="url" class="form-control" id="url" name="url" placeholder="Podaj adres url" value="http://dalvik.nuvo.pl/">
						</div>
						<button type="submit" class="btn btn-primary">Wyślij</button>
					</form>
				</div>
			</div>
		</div>
	</div>
</body>
</html>